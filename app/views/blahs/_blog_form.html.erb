<%= form_with(model: blah) do |form| %>
    <% if blah.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(blah.errors.count, "error") %> prohibited this blah from being saved:</h2>
  
        <ul>
          <% blah.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div>
      <%= form.label :comment, style: "display: block" %>
      <%= form.text_area :comment %>
    </div>
      <%= form.hidden_field :user_id, value: current_user.id %> <!-- This is a hidden field -->
      <%= form.hidden_field :blog_id, value: @blog.id %>
  
    <div class="form-group">
      <%= form.submit %>
    </div>
  <% end %>